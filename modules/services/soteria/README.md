# soteria

Service module for soteria, a polkit authentication agent that enables pop up password prompts for interactive authentication GUIs. Most notably useful for mounting disks that require root privileges in a GUI context, or for network applets that provide tray icons to manage wireless networks.
